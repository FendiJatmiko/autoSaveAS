#### A simple bash script to automatically save a file from a given address
#### a bit different from `curl` or `wget` 
#### this is for my specific use case, which is to download captcha data model for machine learning
